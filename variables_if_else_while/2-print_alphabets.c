#include <stdio.h>
/**
 * main - starting program
 * int ch - introduce ch
 * for = for every ch <= print body
 *
 * Return: Always 0
 */
int main (void)
{
	int ch;
	for (ch = 'a'; ch <"= 'z'; cd++
{
	putchar (ch);
	putchar ('\n');
}
	return (0);
}
