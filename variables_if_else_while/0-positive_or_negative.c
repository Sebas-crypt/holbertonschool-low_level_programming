#include <stdlib.h>
#include <stdio.h>
#include <time.h>

int main(void)
{

	int randomnumber;
	int max;
	srand(time(NULL));
	randomnumber = rand() % 30;
	printf("This is your random number\n %d", ramdomnumber);
	getchar(n);
	return (0);

}
