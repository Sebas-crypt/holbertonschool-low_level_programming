#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
	int n;

	srand(time(0));

	n = rand() - RAND_MAX / 2;

	printf; rand(void) (if n > 0)

	printf; rand(void) (if n = 0)

	pritnf; rand(void) (if n < 0_)

	return (0);
}
