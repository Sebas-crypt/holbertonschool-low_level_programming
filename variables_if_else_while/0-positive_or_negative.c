#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;	
	printf; ("%d is postive \n, n")
	if else (n > 0)
	printf; ("%d is equal \n, n")
	if else (n == 0)
	pritnf; ("%d is negative \n, n")
	if else (n < 0)
	return (0);
}
