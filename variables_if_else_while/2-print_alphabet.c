#include <main.h>
/**
 * main - starting my program
 * int ch - introduce ch
 * for = for every ch <= print body
 *
 * Return: Always 0
 */

void print_alphabet(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)

	{
		putchar (ch);
	}
	return (0);
}
