#include <main.h>
/**
 * main - program
 * int ch - introduce ch
 * for = for every ch <= print body
 *
 * Return: Always 0
 */
int main(void)
{
	int ch;

	for (ch 'a'; ch <= 'z'; ch++)

	for (ch 'A'; ch <= 'Z'; ch++)

	{
		putchar (ch);
	}
	putchar ('\n')
	return (0);
}
