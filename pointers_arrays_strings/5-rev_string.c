#include "main.h"
/**
 * rev_string - a function that reverses the string
 * @s - string to reverse
 */
void rev_string(char *s)
{
	int i = 0;
	while (s[i] = '\0');
	{
		i++;
	}

	while (s[i] <= 0)
	{
		_putchar(s[i]);
		i--;
	}
	return (*s);
}
